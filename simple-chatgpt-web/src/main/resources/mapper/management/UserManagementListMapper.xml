<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="simple.chatgpt.mapper.management.UserManagementListMapper">

    <!-- =======================
         CRUD METHODS
         ======================= -->
    <insert id="insertList" parameterType="simple.chatgpt.pojo.management.UserManagementListPojo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_management_list
        (user_list_name, file_path, original_file_name, description, created_at, updated_at)
        VALUES
        (#{userListName}, #{filePath}, #{originalFileName}, #{description}, NOW(), NOW())
    </insert>

    <update id="updateList" parameterType="simple.chatgpt.pojo.management.UserManagementListPojo">
        UPDATE user_management_list
        SET
            user_list_name = #{userListName},
            file_path = #{filePath},
            original_file_name = #{originalFileName},
            description = #{description},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteList" parameterType="long">
        DELETE FROM user_management_list WHERE id = #{id}
    </delete>

    <select id="findListById" parameterType="long" resultType="simple.chatgpt.pojo.management.UserManagementListPojo">
        SELECT id, user_list_name, file_path, original_file_name, description, created_at, updated_at
        FROM user_management_list
        WHERE id = #{id}
    </select>

    <select id="findAllLists" resultType="simple.chatgpt.pojo.management.UserManagementListPojo">
        SELECT id, user_list_name, file_path, original_file_name, description, created_at, updated_at
        FROM user_management_list
        ORDER BY created_at DESC
    </select>

    <!-- =======================
         SEARCH / PAGINATION
         ======================= -->
    <select id="findLists" parameterType="map" resultType="simple.chatgpt.pojo.management.UserManagementListPojo">
        SELECT id, user_list_name, file_path, original_file_name, description, created_at, updated_at
        FROM user_management_list
        <where>
            <if test="params.userListName != null and params.userListName != ''">
                AND user_list_name LIKE CONCAT('%', #{params.userListName}, '%')
            </if>
            <if test="params.description != null and params.description != ''">
                AND description LIKE CONCAT('%', #{params.description}, '%')
            </if>
        </where>
        <if test="params.sortField != null and params.sortDirection != null">
            ORDER BY ${params.sortField} ${params.sortDirection}
        </if>
        <if test="params.offset != null and params.limit != null">
            LIMIT #{params.offset}, #{params.limit}
        </if>
    </select>

    <!-- =======================
         COUNT FOR PAGINATION
         ======================= -->
    <select id="countLists" parameterType="map" resultType="long">
        SELECT COUNT(*)
        FROM user_management_list
        <where>
            <if test="params.userListName != null and params.userListName != ''">
                AND user_list_name LIKE CONCAT('%', #{params.userListName}, '%')
            </if>
            <if test="params.description != null and params.description != ''">
                AND description LIKE CONCAT('%', #{params.description}, '%')
            </if>
        </where>
    </select>

</mapper>
